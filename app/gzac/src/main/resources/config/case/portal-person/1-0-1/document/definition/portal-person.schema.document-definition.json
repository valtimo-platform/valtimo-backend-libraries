{
    "$id": "portal-person.schema",
    "type": "object",
    "title": "Person",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
        "firstName": {
            "type": "string",
            "maxLength": 15,
            "description": "The person's first name."
        },
        "lastName": {
            "type": "string",
            "maxLength": 15,
            "description": "The person's first name."
        },
        "birthday": {
            "type": "string",
            "format": "date",
            "description": "Birthday"
        }
    },
    "additionalProperties": true
}